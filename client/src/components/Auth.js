import React from 'react';
import Containers from './auth/LoginContainer';

const Auth = (props) => {
    return (
        <div>
            <Containers.LoginContainer />
        </div>
    );
};

export default Auth;